import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    padding: 16,
  },
  TextInput: {
    marginBottom: 16,
  },
  row: {
    flex: 1,
    flexDirection: 'row',
  }
});